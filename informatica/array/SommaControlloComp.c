/*dopo aver chiamato un vettore numerico confrontare la somma delle
componenti di indice pari con la somma delle componenti dispari e dire se
la somma maggiore è divisibile per un assegnato valore k(k=!0)*/

#include <stdio.h>
#include <time.h>
#include <stdlib.h>
#include "libArray.c"
#define DIM 10

int main(){
    int vettore[DIM]={0};
    caricaVett(vettore, DIM);
    
}