/*Calcolare quanti e quali numeri primi sono presenti in un ampio intervallo di valori.
Definizione di numero primo: un numero si dice primo se è divisibile solo per 1 e per se stesso.*/
#include <stdio.h>
int main(){
    int nummax,nummin,cnt=0;
    do{
        printf("inserisci un numero minimo: ");
        scanf("%d", &nummin);
    }while(nummin<=0);
    do{
        printf("inserisci un numero massimo: ");
        scanf("%d", &nummax);
    }while(nummax<=30);
    for(int i; i<=nummax;i++);{
        for(int i; i<=nummin;i++);
    }
    
    
    

    
    



  
}